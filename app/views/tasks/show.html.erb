<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<% if @task.completed %>
    completed
    <% else %> not completed
<% end %>
<%= link_to "Back to list", tasks_path %>
<%= link_to "Edit task", edit_task_path(@task) %>
